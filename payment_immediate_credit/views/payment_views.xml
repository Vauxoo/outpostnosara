<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="payment_acquirer_view_form_inherit_immediate_credit" model="ir.ui.view">
        <field name="name">payment.acquirer.view.form.inherit.immediate_credit</field>
        <field name="model">payment.acquirer</field>
        <field name="inherit_id" ref="payment.acquirer_form"/>
        <field name="arch" type="xml">
            <page name="acquirer_credentials" position="attributes">
                <attribute name="attrs">{'invisible': [('provider', 'in', ['manual', 'transfer', 'immediate_credit'])]}</attribute>
            </page>
            <field name="pre_msg" position="attributes">
                <attribute name="attrs">{'invisible': [('provider', 'in', ['transfer', 'immediate_credit'])]}</attribute>
            </field>
            <field name="done_msg" position="attributes">
                <attribute name="attrs">{'invisible': [('provider', 'in', ['transfer', 'immediate_credit'])]}</attribute>
            </field>
            <field name="cancel_msg" position="attributes">
                <attribute name="attrs">{'invisible': [('provider', 'in', ['transfer', 'immediate_credit'])]}</attribute>
            </field>
        </field>
    </record>
</odoo>
